<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.modernbar"
          path="/org/gnome/shell/extensions/modernbar/"
          gettext-domain="modernbar@gdesh.com">

    <key name="palette" type="s">
      <choices>
        <choice value="tron"/>
        <choice value="iso"/>
        <choice value="clu"/>
        <choice value="rinzler"/>
        <choice value="sark"/>
        <choice value="military"/>
        <choice value="utility"/>
      </choices>
      <default>'tron'</default>
      <summary>Circuit palette</summary>
      <description>Which canon circuitry color the bar wears. The background is
        the same blueish deep black for every choice — only the circuit color
        changes. Values follow the Program lore: tron (blue), iso (white),
        clu (gold), rinzler (orange), sark (red), military (teal green),
        utility (violet). Scriptable via
        `gsettings set org.gnome.shell.extensions.modernbar palette clu`.</description>
    </key>

    <key name="night-mode" type="b">
      <default>false</default>
      <summary>Deprecated day/night alias (use `palette`)</summary>
      <description>Kept so existing scripts keep working: when this key CHANGES,
        the extension maps true → palette 'clu' and false → palette 'tron'.
        Not shown in prefs; new scripts should set `palette` directly.</description>
    </key>

    <key name="theme-popups" type="b">
      <default>true</default>
      <summary>Theme the panel dropdowns</summary>
      <description>Extend the palette to the Quick Settings and clock/calendar
        dropdowns. Off leaves both stock Adwaita (the panel itself is still themed).
        Kept as its own switch because these popups live outside #panel and lean on
        stock GNOME selectors — if a shell upgrade ever breaks them, this is the
        one-flip escape hatch that does not require disabling the extension.</description>
    </key>

    <key name="dismiss-on-leave" type="b">
      <default>true</default>
      <summary>Close dropdowns when the pointer leaves</summary>
      <description>Close the Quick Settings and clock/calendar dropdowns when the
        pointer moves away from them, matching how the metric dropdowns behave.
        Click-outside, Escape and keyboard navigation are unchanged — this only adds
        a reason to close. Dismissal is suppressed while a mouse button is held, so
        dragging a slider past the pane edge is safe, and it only takes effect once
        the pointer has entered the dropdown, so menus opened from the keyboard are
        left alone.</description>
    </key>

    <key name="work-start" type="s">
      <default>'09:00'</default>
      <summary>Work day start time</summary>
      <description>Start of the work day, "HH:MM" 24-hour. Workday % counts from here.</description>
    </key>

    <key name="work-end" type="s">
      <default>'17:00'</default>
      <summary>Work day end time</summary>
      <description>End of the work day, "HH:MM" 24-hour. The metric hides outside these hours.</description>
    </key>

    <key name="show-workday" type="b">
      <default>true</default>
      <summary>Show the Workday % metric</summary>
      <description>Master toggle for the Workday percentage indicator.</description>
    </key>

    <key name="location-label" type="s">
      <default>'Cupertino'</default>
      <summary>Location display/search name</summary>
      <description>City name shown/searched. Geocoded to coordinates when changed in prefs.</description>
    </key>

    <key name="latitude" type="d">
      <range min="-90.0" max="90.0"/>
      <default>37.32300</default>
      <summary>Weather latitude</summary>
      <description>Latitude used for the Open-Meteo weather query. Usually set
        by geocoding the location name in prefs; directly editable there too
        (offline / privacy / geocoder-down fallback).</description>
    </key>

    <key name="longitude" type="d">
      <range min="-180.0" max="180.0"/>
      <default>-122.03220</default>
      <summary>Weather longitude</summary>
      <description>Longitude used for the Open-Meteo weather query. See latitude.</description>
    </key>

    <key name="temperature-unit" type="s">
      <choices>
        <choice value="fahrenheit"/>
        <choice value="celsius"/>
      </choices>
      <default>'fahrenheit'</default>
      <summary>Temperature unit</summary>
      <description>Unit for the weather readout: fahrenheit or celsius.</description>
    </key>

    <key name="weather-refresh-minutes" type="i">
      <range min="5" max="180"/>
      <default>20</default>
      <summary>Weather refresh interval (minutes)</summary>
      <description>How often to re-fetch weather from Open-Meteo.</description>
    </key>

    <key name="show-weather" type="b">
      <default>true</default>
      <summary>Show the Weather metric</summary>
      <description>Master toggle for the weather indicator.</description>
    </key>

    <key name="show-cpu" type="b">
      <default>true</default>
      <summary>Show the CPU % metric</summary>
      <description>Master toggle for the CPU percentage indicator.</description>
    </key>

    <key name="cpu-refresh-seconds" type="i">
      <range min="1" max="10"/>
      <default>3</default>
      <summary>CPU sample interval (seconds)</summary>
      <description>How often to sample /proc/stat for the CPU percentage.</description>
    </key>

    <key name="cpu-warn-percent" type="i">
      <range min="50" max="100"/>
      <default>90</default>
      <summary>CPU warning threshold (percent)</summary>
      <description>At or above this CPU %, the value flares Clu-orange as an alert.</description>
    </key>

    <key name="show-claude" type="b">
      <default>false</default>
      <summary>Show the Claude usage metric (opt-in)</summary>
      <description>Master toggle for the Claude 5-hour usage indicator. Off by
        default: this metric reads the OAuth token Claude Code stores in
        ~/.claude/.credentials.json and sends it to Anthropic's usage endpoint,
        so it only ever runs after being explicitly enabled. While off, the
        credential file is not opened and no request is made.</description>
    </key>

    <key name="claude-refresh-seconds" type="i">
      <range min="10" max="3600"/>
      <default>30</default>
      <summary>Claude usage refresh interval (seconds)</summary>
      <description>How often to query the account usage endpoint. 30s is the
        shipped default because 15s was measured returning 429 rate_limit_error
        repeatedly (worse with more than one shell session polling at once). The
        floor is 10s for experimentation; failures keep the last value, cache it
        across restarts and retry, so too-fast degrades to "stale" rather than
        "gone".</description>
    </key>

    <key name="claude-cache-percent" type="i">
      <range min="-1" max="1000"/>
      <default>-1</default>
      <summary>Last known 5-hour utilization (cache)</summary>
      <description>-1 = no cached value. Internal; not shown in prefs.</description>
    </key>

    <key name="claude-cache-time" type="x">
      <default>0</default>
      <summary>Unix time of the cached utilization</summary>
      <description>Epoch seconds. Readings older than an hour remain visible but
        render as stale. Internal; not in prefs.</description>
    </key>

    <key name="claude-warn-percent" type="i">
      <range min="50" max="100"/>
      <default>80</default>
      <summary>Claude usage warning threshold (percent)</summary>
      <description>At or above this 5-hour utilization %, the value flares Clu-orange.</description>
    </key>

    <key name="show-codex" type="b">
      <default>false</default>
      <summary>Show the Codex usage metric (opt-in)</summary>
      <description>Master toggle for Codex account usage. Off by default: this
        metric reads the ChatGPT access token and account ID Codex stores in
        ~/.codex/auth.json and sends them only to ChatGPT's usage endpoint.
        While off, the credential file is not opened and no request is
        made.</description>
    </key>

    <key name="codex-refresh-seconds" type="i">
      <range min="30" max="3600"/>
      <default>60</default>
      <summary>Codex usage refresh interval (seconds)</summary>
      <description>How often to query account usage. Failures keep the last
        value and retry with backoff.</description>
    </key>

    <key name="codex-cache-percent" type="i">
      <range min="-1" max="1000"/>
      <default>-1</default>
      <summary>Last known Codex utilization (cache)</summary>
      <description>-1 = no cached value. Internal; not shown in prefs.</description>
    </key>

    <key name="codex-cache-time" type="x">
      <default>0</default>
      <summary>Unix time of the cached Codex utilization</summary>
      <description>Epoch seconds. Readings older than an hour remain visible but
        render as stale. Internal; not in prefs.</description>
    </key>

    <key name="codex-warn-percent" type="i">
      <range min="50" max="100"/>
      <default>80</default>
      <summary>Codex usage warning threshold (percent)</summary>
      <description>At or above this utilization, the displayed window flares
        Clu-orange.</description>
    </key>

  </schema>
</schemalist>
